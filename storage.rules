rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /profile_photos/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == fileName.replace(".jpg", "");
    }
  }
}
